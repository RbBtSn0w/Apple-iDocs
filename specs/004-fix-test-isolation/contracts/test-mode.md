# Contract: 测试模式与集成测试开关

## 目的
定义默认测试与网络集成测试的分层行为与启用方式。

## 配置入口
- **环境变量**: `IDOCS_INTEGRATION_TESTS`
- **启用条件**: 值为 `1` 时启用网络集成测试
- **默认行为**: 未设置或非 `1` 时，网络集成测试应被跳过

## 运行语义
- 默认测试必须不访问外部网络
- 集成测试允许访问外部网络，失败应提供清晰可诊断信息
- 文档需说明两种测试模式的运行方式
- 当使用 `swift test --filter IntegrationTests` 时，测试应自动视为集成模式
